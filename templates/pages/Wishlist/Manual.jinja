{#def
    results: list[ManualBookRequest],
    user: DetailedUser,
    counts: WishlistCounts,
    sort = "title",
    sort_dir = "asc",
#}

<WishlistLayout title="Manual" user={{ user }} counts={{ counts }} page="manual">
<div class="overflow-x-auto flex-1 min-h-0 border-b pb-2 border-b-base-200">
    <Wishlist.ManualWishlist results={{ results }} user={{ user }} counts={{ counts }} sort={{ sort }} sort_dir={{ sort_dir }} />
</div>
</WishlistLayout>
